# 产品需求文档 (PRD) - Data to Viz (中文重制版)

## 1. 项目概述

### 1.1. 项目背景
原项目 `data-to-viz.com` 是一个经典的数据可视化决策指南，但其技术架构陈旧（R Markdown + 静态 SVG），且以静态图片为主，缺乏交互性，移动端体验极差。
本项目旨在重构一个**现代化的、交互式的、中文版**数据可视化指南。

### 1.2. 核心价值
帮助开发者、数据分析师或设计师，根据手头的数据类型（如：1个数值变量、2个分类变量等），快速找到最合适的图表类型，并提供代码实现参考。

### 1.3. 核心原则
*   **交互优先：** 图表不再是静态图片，而是可交互的代码（ECharts）。
*   **移动端适配：** 复杂的决策树逻辑必须在手机上也能顺畅操作。
*   **极致视觉：** 核心决策树必须具备顶级的交互体验（Living Tree），超越原版的静态 SVG。
*   **结构扁平：** 拒绝复杂的图表嵌套（Tree inside Chart），保持代码可控。

---

## 2. 用户流程 (User Flow)

1.  **入口选择：** 用户进入首页，面对一个宏观的数据分类选择（数值、分类、地图、网络、时间序列）。
2.  **决策引导：**
    *   **桌面端：** 用户在一个沉浸式的“交互树”中，通过鼠标悬停和点击，跟随发光的路径找到目标。
    *   **移动端：** 用户通过回答“向导式问题”，一步步缩小范围。
3.  **结果预览：** 鼠标悬停在叶子节点上，节点内部实时渲染微型图表（Sparkline）进行预览。
4.  **深度学习：** 用户点击节点，进入详情页，查看完整的交互式图表、适用场景、错误避坑指南。

---

## 3. 功能模块详解

### 3.1. 首页与决策导航系统 (Core)

这是重构的灵魂。拒绝通用的流程图库，采用纯手写定制组件，打造 **"Living Tree" (有生命力的树)**。

#### A. 桌面端：定制交互树 (Custom Living Tree)
*   **技术方案：** **SVG + React + Framer Motion**。
*   **布局算法：** 采用 Tidy Tree (整洁树) 布局算法，确保节点自动排布，互不重叠。
*   **视觉与交互细节：**
    *   **连线 (Links)：** 使用 SVG 贝塞尔曲线。当鼠标划过路径时，线条产生“电流传导”般的发光动效。
    *   **节点 (Nodes)：** 纯 React 组件，绝对定位覆盖在 SVG 之上。
    *   **聚焦模式 (Focus Mode)：** 点击某一大类（如“数值”）时，非相关分支自动变暗/收缩，当前分支放大并平滑移动至视图中心。
    *   **微图表预览 (Sparkline Preview)：**
        *   **默认状态：** 展示精简的 SVG 矢量图标 + 图表名称。
        *   **悬停状态：** 节点瞬间放大，内部**实时渲染**一个微型的 ECharts 图表（使用随机/模拟数据），让用户直观感受数据形态。

#### B. 移动端：向导式问卷 (Wizard)
*   **响应式策略：** 手机屏幕无法承载大树布局，通过 CSS Media Query 热切换为完全不同的视图组件。
*   **交互逻辑：**
    *   **Step 1:** “你主要的数据类型是什么？” (按钮：数值 / 分类 / 地图...)
    *   **Step 2:** (若选数值) “你有几个变量？” (按钮：1个 / 2个 / 多个)
    *   **Step 3:** (若选多个) “数据是有序的吗？” (按钮：是 / 否)
    *   **Result:** 推荐结果列表（卡片流）。

### 3.2. 图表详情页 (Detail Page)

*   **页面结构：**
    1.  **交互演示区 (Top/Left):**
        *   加载一个真实的 ECharts 实例。
        *   **数据切换：** 提供简单的按钮切换数据集（例如：切换“随机数据”和“原项目CSV数据”），让用户感受图表变化。
    2.  **定义与用途 (Definition & What for):**
        *   Markdown 渲染的说明文案。
    3.  **常见错误 (Caveats):**
        *   展示“错误用法”和“正确用法”的对比图。

#### 3.3. 搜索与索引 (Global Search)
*   提供全局搜索框，支持搜索图表名称或数据特征。

### 3.4. 全局大屏双主题系统 (Big-screen Dark & Creamy Light Theme)
作为面向现代开发者的工具，系统提供极富沉浸感的全站亮/暗色双主题：
*   **大屏暗色模式 (Big-screen Dark)**：专为夜间调试与大屏投影设计。以深邃暗青色为底色，结合青色霓虹发光曲线，增强数据的科技感和视觉冲击力。
*   **奶油微光亮色模式 (Soft Geek Creamy Light)**：专为日间高亮阅读设计。以奶油暖灰为底色，结合天空蓝极细背景网格以及海蓝至靛蓝强渐变节点，配合毛玻璃软投影效果，营造极其温润而精致的阅读体验。
*   **零报错动态热切换**：系统拦截了底层图形引擎渲染周期，确保用户切换主题时，全站 80+ G2 交互图表与 G6 决策树能在 Canvas 层面秒级销毁、无感重绘、完美对齐全局色板。

---

## 4. 技术规格 (Technical Specs)

### 4.1. 核心技术栈
*   **框架:** Next.js 16 (App Router)
*   **样式:** Tailwind CSS
*   **动画:** Framer Motion / G6 React-Node Transition
*   **可视化:** AntV G2 v5 (用于详情页交互式图表与微预览) & AntV G6 v5 (用于 Living Tree 决策树导航)
*   **工具库:** @antv/g6 内置 Layout 算法 (用于计算树节点的 x,y 坐标)

### 4.2. 数据存储策略
*   **Tree Logic JSON:** 扩展节点属性以支持高级交互。
    ```json
    {
      "id": "numeric",
      "label": "数值型",
      "children": [
        {
          "id": "violin",
          "label": "小提琴图",
          "previewConfig": { "chartType": "violin", "mockType": "normal_dist" }
        }
      ]
    }
    ```

---

## 5. 重构难点与解决方案

| 难点 | 原版做法 | 中文重制版方案 | 优势 |
| :--- | :--- | :--- | :--- |
| **视觉陈旧** | 静态 SVG 图片 | **SVG + Framer Motion 动态渲染** | 现代感强，支持路径发光、节点生长动画 |
| **交互单一** | 仅点击跳转 | **Hover 实时预览 (Sparkline)** | 极大地降低了用户的认知成本，所见即所得 |
| **移动端体验** | 需缩放查看大图 | **View层热切换 (Tree -> Wizard)** | 完美的响应式体验，逻辑复用，视图分离 |
| **图形引擎亮暗热切换** | 切换主题页面闪烁，Canvas 内部轴文字仍为原色且报错崩溃 | **无侵入拦截渲染，配以 React Key Remount** | 拦截 `G2Chart`/`G6Graph` 渲染，以 shallow clone 应对 Frozen 属性，利用 `key={theme}` 动态触发重置，达成无缝流畅切换 |

---

## 6. 第一阶段 (MVP) 交付目标：核心交互验证原型

**目标：** 不急于搭建全站，先构建一个独立的 Demo 页面，验证“Living Tree”方案的可行性和视觉效果。

1.  **原型环境：** 搭建一个最小化的 Next.js + Tailwind 环境。
2.  **数据模拟：** 构造 `numeric` (数值型) 分支的 JSON 数据。
3.  **核心组件开发：**
    *   实现基于 `d3-hierarchy` 的节点坐标计算 hook。
    *   实现 SVG 贝塞尔曲线连接组件。
    *   实现支持 Hover 展开并渲染 ECharts Sparkline 的节点组件。
4.  **验证标准：**
    *   树形结构渲染正确。
    *   连线平滑，有基础动效。
    *   鼠标悬停叶子节点时，能丝滑地展示出 ECharts 迷你图，无明显卡顿。