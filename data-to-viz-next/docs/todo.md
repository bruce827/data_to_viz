# 待办记录

## 2026-03-10

- [ ] `chart-net-force` 应用场景图初始布局仍异常重叠
  - 决策树路径：网络关系 → 基础连接关系 → 力导向图
  - 节点 ID 路径：`root/q-net-basic/chart-net-force`
  - 页面路由：`/graph/network_force`
  - 现象：页面首次打开时，节点仍聚集在少数位置，需要手动拖拽后才能展开到可用布局。
  - 当前状态：已尝试补初始散点坐标与加大 force 斥力，但未完全解决。
  - 下次排查方向：检查 G6 `force` 布局是否忽略节点初始 `style.x/y`，必要时改为固定初始定位 + 首帧后再启动布局，或改为更稳定的 `d3-force`/预布局方案。
