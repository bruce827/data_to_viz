# 2026-03-06 数值与类别决策树补齐收尾记录

## 本次目标
- 继续完成 `tree-catnum` 的剩余 story 页面开发与独立化。
- 严格执行“一页一节点 ID”规则，避免不同叶子节点复用同一路由。
- 按反馈修正颜色映射、标签显示、Hydration 等稳定性问题。

## 本次完成内容
1. 补齐并独立化 `tree-catnum` 剩余叶子节点
- 新增独立页面并完成场景图：
  - `/graph/arc`
  - `/graph/sankey-structure`
  - `/graph/network-structure`
  - `/graph/chord-structure`
  - `/graph/treemap-structure`
  - `/graph/sunburst-structure`
  - `/graph/circularpacking-structure`
  - `/graph/venn-structure`
  - `/graph/circularpacking-set-structure`
  - `/graph/upset-structure`
- 同步更新 `src/data/tree-catnum.json`，为所有叶子节点补齐或改正 `storyPath`。

2. 按业务语义重构多个应用场景图
- `Treemap`：改为 `TREE` 对应的“行业 -> 子行业”风险敞口集中度，并补充数据表。
- `Sunburst`：按用户要求重构为更能体现旭日图特点的多层级结构，改成“产品 -> 渠道 -> 客群”。
- `Circular Packing (Hierarchy)`：按 `CPAK` 改成“客群 -> 风险等级”层级结构，并在图上补数值标签。
- `Venn`：改成 `VENN` 对应的小微预警标签三集合交集。
- `Circular Packing (Set)`：改成 `CPAKS` 对应的多类合规风险事件规模对比。
- `UpSet`：改成 `UPST` 对应的信用卡多风险标签交集。

3. 修正颜色映射与图形可读性问题
- `Treemap` 场景图颜色映射改为显式函数映射，避免层级数据字段路径失效。
- `Sunburst` 场景图颜色改成“每个产品一个色系，旗下渠道/客群跟随同色系”。
- `Circular Packing` 场景图增加图内数值，并下调显示阈值，保证小圆也能显示简写数值。

4. 修复 UpSet Hydration 问题
- `UpSetJS` 在 SSR/CSR 生成随机 className，导致 hydration mismatch。
- 两个 UpSet 组件都改为仅在客户端挂载后再渲染真实图表，避免首帧不一致。

## 规则补充
- 已在 `AGENTS.md` 增加颜色映射规则：
  - 颜色映射必须可追溯、可验证。
  - 层级图优先使用显式函数映射，不依赖不稳定字段路径。
  - 改完后必须人工核对颜色是否真的跟随数据变化。

## Bugfix 记录补充
- 已在 `docs/bugfix.md` 新增：
  - `2026-03-06 Treemap 场景颜色映射未生效`
- 记录了现象、根因、修复方案与验证要点。

## 当前状态（数值与类别决策树）
- `tree-catnum` 共 `16` 个叶子节点。
- 当前状态：`16/16` 均已实现。
- 已核对：
  - 每个节点都有 `storyPath`
  - 每个 `storyPath` 页面文件都存在
  - 每个页面都包含对应叶子节点 ID
  - 无重复 `storyPath`

## 关键变更文件
- `src/data/tree-catnum.json`
- `src/components/viz/charts/ArcScenarioG2.tsx`
- `src/components/viz/charts/SankeyStructureScenarioG2.tsx`
- `src/components/viz/charts/NetworkStructureScenarioG6.tsx`
- `src/components/viz/charts/ChordStructureScenarioG2.tsx`
- `src/components/viz/charts/TreemapStructureScenarioG2.tsx`
- `src/components/viz/charts/SunburstStructureScenarioG2.tsx`
- `src/components/viz/charts/CircularPackingStructureScenarioG2.tsx`
- `src/components/viz/charts/VennStructureScenarioG2.tsx`
- `src/components/viz/charts/CircularPackingSetStructureScenarioG2.tsx`
- `src/components/viz/charts/UpsetStructureScenarioG2.tsx`
- `src/components/viz/charts/UpsetSetRelationG2.tsx`
- 对应的 `src/app/graph/*/page.tsx` 路由页面
- `AGENTS.md`
- `docs/bugfix.md`

## 下次衔接建议
1. `tree-catnum` 已完成，可切换到地图决策树开发。
2. 下一步输入已确认：`docs/map-deep-research-report.md`。
3. 继续沿用当前规则：
   - 先看 `docs/bugfix.md`
   - 每完成一个 story 由用户确认
   - report 数据若不能直接满足图表结构，先讨论再补数
