# 会话总结（2026-03-03）- Story 模板与直方图场景化

## 今日目标
先完成 Story 详情页模板定型，再把数值树中的直方图页面改造成“节点 ID 对齐的业务场景版”，为后续批量实现全部数值 story 打样。

## 已完成内容
- 直方图页面内容改造：
  - 将原“实现方式”替换为“应用场景”。
  - 基于 `docs/deep-research-report.md` 提取并接入 `chart-hist` 对应场景（`root/q-1num/chart-hist`）。
  - 按报告示例数据实现 DPD 分箱直方图（账户数分布 + 关键指标 tooltip）。
  - 按“节点 ID 一一对应”原则，移除 `chart-hist-few` 的场景展示，当前页面仅保留单场景版本。
- Story 通用布局模板改造（`StoryLayout`）：
  - Hero 区域明显收敛，避免喧宾夺主。
  - icon 与 title 改为同一行，形成紧凑标题区。
  - 内容视觉重心下移到正文与图表区域。
- 大纲导航能力接入：
  - `StoryLayout` 新增可选 `outlineItems`，支持左侧大纲导航。
  - `StorySection` 新增可选 `id`，支持锚点跳转。
  - 直方图页已接入大纲并完成 section 锚点配置。
- 移动端适配：
  - 大纲导航仅桌面显示（`hidden lg:block`），移动端自动隐藏。
  - 调整 header/content/footer 以及 section/chart 的 mobile-first 间距与字号。

## 当前模板形态（可复用）
- 页面结构：`Header -> Hero(紧凑标题) -> Main(可选左侧大纲 + 正文) -> Footer`
- 正文组件：`StorySection` + `ChartWrapper` 组合
- 场景写法：一个 story 页面默认对应一个决策树叶子节点 ID（报告与树节点对齐）

## 验证结果
- `npx eslint`：通过（涉及文件）
- `npx tsc --noEmit`：通过

## 后续执行建议（数值决策树批量实现）
- 严格按“节点 ID 对齐”从 `deep-research-report` 抽取场景，保持单页单节点。
- 每个 story 至少包含：定义、何时使用、常见误区、应用场景（含对应图表）。
- 新增页面默认接入 `outlineItems + StorySection id`，并保持移动端隐藏大纲策略。
