# 2026-03-05 类别决策树 story 补齐记录

## 本次目标
- 继续 `docs/cate-deep-research-report.md` 对应的类别决策树 story 开发。
- 按规则逐页推进：先读 `docs/bugfix.md`，每页完成后可确认。
- 交互示例图统一“下载到本地后加载”。

## 本次完成内容
1. 完成 `chart-chord-set`（和弦图）story 重构
- 页面改为中文业务语境（非通用英文模板）。
- 应用场景图改为 report 对应数据（产品交叉销售路径）。
- 交互示例图替换为本地图并处理缓存问题（改用新文件名引用）。
- 在“什么是和弦图”下追加结构化说明表（图表类型/适合数据/映射等）。

2. 完成 `chart-network-set`（关系图）story 重构
- 页面改为中文业务语境（集团互保链与风险传染）。
- 新增场景组件，使用 report 的 nodes/edges 数据。
- 场景中补充图上语义与下方数据表，支持业务解读。

3. 补齐遗漏叶子节点 `chart-circular-packing`
- 发现 `chart-circular-packing` 与 `chart-circular-packing-set` 之前共用同一路由，违反“一页一节点 ID”。
- 新增独立路由页面：`/graph/circularpacking-hierarchy`（层级关系语境）。
- 新增独立场景组件（贷款组合层级结构）并绑定 report 对应数据。
- 更新 `src/data/tree-categoric.json`：`chart-circular-packing.storyPath` 指向新路由。

## 关键规则执行结果
- 已遵循：report 数据若可直接映射则直接使用；不满足时先讨论再补数。
- 已遵循：交互示例图按“下载到本地”方式处理，不使用远程 URL 直链。
- 已遵循：每个 story 完成后回报中文路径、节点 ID 路径、页面路由。

## 叶子节点覆盖状态（分类决策树）
- 当前结果：`14/14` 全部完成。
- 本次补齐的关键差异点：`chart-circular-packing` 已与 `chart-circular-packing-set` 分离为独立 story 页面。

## 关键变更文件
- `src/app/graph/chord/page.tsx`
- `src/components/viz/charts/ChordScenarioG2.tsx`
- `src/app/graph/network/page.tsx`
- `src/components/viz/charts/NetworkScenarioG6.tsx`
- `src/app/graph/circularpacking-hierarchy/page.tsx`
- `src/components/viz/charts/CircularPackingHierarchyScenarioG2.tsx`
- `src/data/tree-categoric.json`
- `public/images/graph/chord/interactive-example-2.png`

## 下次衔接建议
1. 在浏览器端做一次分类树全链路点击回归（从树节点进入 story，检查路由与节点一致性）。
2. 若进入下一棵树（如 `catnum` 或 `numeric`），继续沿用本次规则：先读 `bugfix`、按叶子节点逐页确认。
